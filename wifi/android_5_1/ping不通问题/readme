我们6356s在使用过程中遇到一个下面的问题，机器成了gms。麻烦帮忙分析一下：

1.设备在连接局域网内的wifi后，亮屏或者充电状态电脑可以正常ping通设备
2.在上面状态基础上灭屏3分钟，释放上层锁，继续插着usb充电任然可以ping通设备
3.通过adb shell dumpsys battery unplug，将usb设置为拔出(只是java层能识别为拔出，底层任然是插入状态)，过几秒后就不能ping通设备了，此时adb shell ifconfig任然可以看到有ip

4.在相同机器上，移除gms后，上面现象消失

由于gms是google的软件包，我们无法看到源码也无法进行修改，能否从底层wifi状态上来判断gms对wifi进行了什么设置呢？ 

---------------------------------------------------------------------------------------------------------------------------
已经找到了不能ping通的原因了， 应该是我们在装了gms后，系统的Doze功能被enable，导致kernel里面按照Doze规则过滤丢弃了ping包，
所以不能ping通，在复现问题时通过命令disable Doze后，就ok了

kernel里面加log，跟到netfilter里面了，结果netfilter的函数在调用后就没有返回，  netfilter这个里面基本上都是防火墙相关的
